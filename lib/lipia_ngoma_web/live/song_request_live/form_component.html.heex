<div>
  <h2><%= @title %></h2>

  <.form
    let={f}
    for={@changeset}
    id="song_request-form"
    phx-target={@myself}
    phx-change="validate"
    phx-submit="save"
  >
    <%= hidden_input(f, :user_id, value: @user.id) %>
    <%= label(f, :name) %>
    <%= text_input(f, :name) %>
    <%= error_tag(f, :name) %>

    <%= label(f, :phone_number) %>
    <%= text_input(f, :phone_number) %>
    <%= error_tag(f, :phone_number) %>

    <%= label(f, :price) %>
    <%= number_input(f, :price) %>
    <%= error_tag(f, :price) %>

    <%= label(f, :songrequestid) %>
    <%= text_input(f, :songrequestid) %>
    <%= error_tag(f, :songrequestid) %>

    <%= label(f, :songname) %>
    <%= text_input(f, :songname) %>
    <%= error_tag(f, :songname) %>

    <div>
      <%= submit("Save", phx_disable_with: "Saving...") %>
    </div>
  </.form>
</div>
